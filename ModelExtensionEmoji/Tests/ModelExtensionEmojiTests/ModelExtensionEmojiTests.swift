import XCTest
@testable import ModelExtensionEmoji

final class ModelExtensionEmojiTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ModelExtensionEmoji().text, "Hello, World!")
    }
}
